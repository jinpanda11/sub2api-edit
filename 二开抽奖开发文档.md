Sub2API 二开 - 每日盲盒抽奖功能 开发文档基于截图完整逆向梳理的功能规格（可直接用于二开实现）。技术栈对齐官方 Sub2API：Go 后端（Ent ORM + PostgreSQL + Redis）+ Vue 3 前端。时区统一使用 Asia/Shanghai。1. 功能概述每日盲盒抽奖是用户侧激励玩法，用于提升登录活跃与充值转化。核心卖点：每次必中（无空奖）
中奖余额即时入账账户余额（US$）
抽奖次数通过「登录 + 充值/兑换码」获取
未使用次数跨天保留，无总上限（仅充值来源有每日上限）

截图关键文案：最高可得 US$2.00 余额奖励
每次必中 · 中奖余额从 US$0.10 起
登录、充值和兑换码入账都能获得抽奖次数，中奖余额即时到账

2. 业务规则（核心逻辑）2.1 抽奖次数获取规则来源
规则
每日上限
发放时机
备注
登录奖励
按中国时区（Asia/Shanghai）每天成功登录赠送 +1 次
1 次
登录后自动发放
当日只发一次
充值/兑换码奖励
每充值或兑换码入账 10 个余额单位 赠送 +1 次
5 次
入账成功即时发放
仅统计当日该来源
合计
-
无总上限
-
未使用次数持续保留，不设账户累计次数或钱包总次数上限

今日充值奖励进度显示：0/5（仅充值/兑换码来源）
可用次数独立计数（截图右侧「可用次数 0 次」）
登录奖励每日自动发放；充值奖励按实际入账金额实时计算（向下取整，例：充值 25 → +2 次）

2.2 抽奖规则每次抽奖消耗 1 次可用次数
必中：随机生成余额奖励，范围 US$0.10 ~ US$2.00
奖励金额即时增加到用户余额（截图「当前余额 US$10.48」）
支持连续抽（「再抽一次」按钮，有次数时可用）
无次数时按钮置灰显示「暂无抽奖次数」，并引导「去充值 →」

2.3 时间与重置所有「今日」计算均按 Asia/Shanghai 时区
登录奖励与充值上限每日 00:00（上海时区）重置
未使用次数不重置，永久保留直到使用

2.4 其他规则（截图底部）每天按中国时区成功登录，赠送 1 次
每充值或兑换码入账 10 个余额单位，赠送 1 次
充值获得的抽奖次数按中国时区每天最多 5 次

每次必中，开出的余额奖励会自动进入账户余额

3. 用户界面（UI 结构复刻）3.1 顶部 Banner（渐变蓝绿）左侧礼物盒图标 + 「最高可得 US$2.00 余额奖励」
右侧卡片：可用次数：大号数字 + 「次」
今日充值奖励进度条：0/5
时区说明 + 今日日期

3.2 中奖结果条（浅黄）恭喜文案 + 中奖金额（US$1.00）
右侧当前余额 + 「再抽一次」按钮

3.3 左侧主区域「开启今日盲盒」大礼物盒插画
状态提示「今日 0 次机会」
主按钮：有次数时「开启盲盒」/ 无次数时「暂无抽奖次数」
下方「去充值 →」链接
底部三步说明卡片（登录 / 充值 / 上限）

3.4 右侧「参与资格」卡片登录奖励：+1（绿色勾）
充值与兑换码奖励：0/5
每日次数规则：5 次
底部三个小卡片：+1 / 10:1 / 5
底部蓝色提示条：「每次必中，开出的余额奖励会自动进入账户余额。」

整体风格：白色卡片 + 圆角 + 轻阴影 + 绿色/蓝色强调色，与 Sub2API 现有 UI 保持一致。4. 数据模型建议（Ent Schema）go

// LotteryChance 用户抽奖次数
type LotteryChance struct {
    ID              int
    UserID          int
    AvailableCount  int       // 当前可用次数（跨天保留）
    TodayRechargeCount int    // 今日已获得充值来源次数（0-5）
    LastLoginDate   time.Time // 上次登录奖励发放日期（上海时区）
    UpdatedAt       time.Time
}

// LotteryRecord 抽奖记录
type LotteryRecord struct {
    ID          int
    UserID      int
    Amount      float64   // 中奖金额（US$）
    Source      string    // login / recharge
    CreatedAt   time.Time
}

// SystemSetting 新增配置项
// lottery_enabled          bool
// lottery_min_amount       float64  // 0.10
// lottery_max_amount       float64  // 2.00
// lottery_recharge_unit    int      // 10
// lottery_recharge_daily_max int    // 5
// lottery_login_reward     int      // 1

5. API 接口设计（建议路径）方法
路径
说明
GET
/api/v1/lottery/status
获取当前可用次数、今日充值进度、是否已领登录奖励、当前余额
POST
/api/v1/lottery/draw
执行抽奖（消耗1次，返回中奖金额 + 新余额）
GET
/api/v1/lottery/records
抽奖历史（分页）
内部
充值/兑换码成功回调
自动增加次数（按规则）
内部
登录成功钩子
检查并发放登录奖励

请求/响应示例（简化）：json

// GET /status
{
  "available_count": 0,
  "today_recharge_count": 0,
  "today_recharge_max": 5,
  "login_rewarded_today": true,
  "current_balance": 10.48,
  "min_prize": 0.10,
  "max_prize": 2.00
}

// POST /draw
{
  "success": true,
  "prize_amount": 1.00,
  "new_balance": 11.48,
  "remaining_count": 0
}

6. 后端实现要点次数发放：登录钩子：查询 LastLoginDate，若非今日则 +1 并更新日期
充值/兑换码：amount / 10 向下取整，且不超过当日剩余额度

抽奖逻辑：事务：扣次数 → 生成随机金额（建议均匀分布或可配置权重）→ 增加余额 → 写记录
使用 Redis 分布式锁防并发重复抽

随机金额：min + rand.Float64() * (max - min)，保留 2 位小数
时区处理：所有日期比较使用 time.LoadLocation("Asia/Shanghai")
防刷：同一用户抽奖接口限流（建议 1 次/秒），IP 维度可选

7. 前端实现要点（Vue 3）页面路由：/lottery 或嵌入用户中心
状态管理：Pinia store 缓存 status
动画：开启盲盒时礼物盒抖动 + 结果弹出
实时刷新：抽奖成功后更新余额与次数
无次数时按钮禁用并引导充值页面
响应式适配移动端

8. 管理后台配置（建议新增）开关：启用/关闭抽奖功能
奖池：最低/最高金额
规则参数：充值单位、每日上限、登录奖励次数
数据统计：今日参与人数、总发放金额、平均中奖金额

9. 边缘情况与安全用户余额为负时仍可抽（奖励可抵扣）
并发抽奖使用乐观锁或 Redis 锁
充值回滚时同步扣减已发放次数（可选，复杂）
日志完整记录每一次发放与抽奖
配置变更实时生效（Redis 缓存 + 定时刷新）

10. 开发优先级与里程碑P0：数据模型 + 次数发放逻辑（登录/充值钩子）+ 抽奖接口
P1：前端页面完整复刻 + 动画
P2：管理后台配置 + 数据统计
P3：历史记录、防刷优化、多语言

此文档可直接作为二开需求说明书使用。

