Sub2API 二开 - 每日消费排行榜功能 开发文档基于你的需求整理的完整开发规格，可直接对接现有 Sub2API 二开（Go + Ent + Vue 3）。与之前的「每日盲盒抽奖」功能风格保持一致，侧边栏新增入口，公开可见。1. 功能概述名称：每日消费排行榜
位置：侧边栏新增菜单项（建议放在「用户中心」或「活动」分组下，图标可用排行榜/奖杯）
可见性：任何人可见（未登录也可访问，无需鉴权）
展示内容：按当日消费金额（API 调用实际扣费金额，单位 US$）从高到低排序，仅显示前 20 名
数据时效：按 Asia/Shanghai 时区计算「今日」，每日 00:00 自动切换新榜单
隐私要求：用户信息强制脱敏，仅展示脱敏邮箱，不显示用户名、ID、真实邮箱或其他敏感信息

2. 业务规则项目
规则
排序依据
用户当日累计消费金额（从余额扣减的真实费用，包含所有模型调用）
展示数量
固定 Top 20（不足 20 人则按实际人数显示）
时间范围
当日 00:00:00 ~ 23:59:59（Asia/Shanghai）
更新方式
实时或准实时（建议消费发生后异步更新排行缓存）
并列处理
金额相同按用户注册时间或 ID 升序
空数据
当日无消费时显示「今日暂无排行数据」
历史
仅展示当日，不保留历史榜单（可后续扩展）

消费定义（对齐 Sub2API 现有计费）：仅统计用户通过平台 API Key 产生的实际扣费金额
不包含充值、赠送、抽奖奖励等非消费行为
金额精确到小数点后 4 位或按现有账单精度

3. 邮箱脱敏规则（强制）仅展示脱敏后的邮箱，格式严格如下：取邮箱本地部分（@ 前面）的前 2 位 + 后 2 位
中间部分用固定占位符或 * 填充（推荐 *，长度自适应）
域名完整保留
示例：原始：ab123456cd@qq.com → ab****cd@qq.com
原始：testuser@example.com → te****er@example.com
原始：a@b.com（过短）→ a*@b.com 或 **@b.com（特殊处理）
你给的示例风格：xx123456xx@qq.com（即前2 + 中间占位 + 后2）

实现建议（Go 伪代码）：go

func MaskEmail(email string) string {
    parts := strings.Split(email, "@")
    if len(parts) != 2 {
        return "***@***"
    }
    local, domain := parts[0], parts[1]
    if len(local) <= 4 {
        return local[:1] + "***@" + domain  // 过短保护
    }
    return local[:2] + strings.Repeat("*", len(local)-4) + local[len(local)-2:] + "@" + domain
}

最终展示效果必须类似：xx****xx@qq.com（与你示例一致）。4. 用户界面（UI）4.1 侧边栏新增菜单：消费排行榜（图标建议 Trophy 或 BarChart）
点击进入独立页面 /ranking 或 /consumption-rank

4.2 排行榜页面顶部标题：「今日消费排行榜」（带日期，如 2026-08-19）
表格或卡片列表（推荐表格）：排名
用户（脱敏邮箱）
今日消费（US$）
1
xx****xx@qq.com
128.4567
2
ab****cd@gmail.com
95.3200
...
...
...
20
...
...

样式：与现有 Sub2API 白色卡片风格一致，前三名可加金银铜图标或高亮
底部提示：「数据按 Asia/Shanghai 时区统计，每日本日 00:00 重置。信息已脱敏保护隐私。」
响应式：移动端友好，支持下拉刷新

5. 数据模型建议（Ent Schema）go

// DailyConsumptionRank 每日排行缓存（可选，提升性能）
type DailyConsumptionRank struct {
    ID          int
    RankDate    time.Time   // 日期（仅年月日，上海时区）
    UserID      int
    MaskedEmail string      // 已脱敏邮箱
    Amount      float64     // 当日消费金额
    Rank        int         // 1-20
    UpdatedAt   time.Time
}

// 索引：RankDate + Rank 唯一，RankDate + Amount 降序

也可不落库，直接实时从 usage / billing 表聚合查询（Sub2API 已有用量记录）。6. API 接口设计方法
路径
鉴权
说明
GET
/api/v1/public/ranking/consumption
无（公开）
获取今日 Top 20

响应示例：json

{
  "date": "2026-08-19",
  "timezone": "Asia/Shanghai",
  "list": [
    {
      "rank": 1,
      "masked_email": "xx****xx@qq.com",
      "amount": 128.4567
    },
    // ... 最多 20 条
  ],
  "updated_at": "2026-08-19T14:30:00+08:00"
}

7. 后端实现要点数据来源：从现有用量/账单表（如 usage_logs 或 transactions）按 user_id + 当日时间范围 SUM(cost) 聚合。
性能优化：使用 Redis Sorted Set（key: rank:consumption:2026-08-19，score = 金额）实时维护
消费发生时（扣费成功后）异步 ZINCRBY
查询时直接 ZREVRANGE 0 19 + 批量获取脱敏邮箱

脱敏：查询时实时计算，或缓存时已存脱敏结果。
时区：所有时间计算强制 time.LoadLocation("Asia/Shanghai")。
缓存：排行结果缓存 1-5 分钟，避免高频查询打库。
空处理：无数据时返回空数组 + 提示文案。

8. 前端实现要点（Vue 3）侧边栏：在现有 Sidebar 组件新增菜单项，路由指向排行页。
页面组件：ConsumptionRank.vue使用 el-table 或自定义卡片列表
前三名特殊样式（金色/银色/铜色）
自动轮询或手动刷新（可选）

无需登录状态判断，直接调用公开接口。

9. 管理后台（可选扩展）开关：是否启用排行榜
查看完整（未脱敏）数据（仅管理员）
手动刷新缓存按钮

10. 安全与边缘情况公开接口限流（IP + 全局 QPS）
脱敏后邮箱不可反向推断真实邮箱
用户无邮箱时跳过该用户或显示 ***@***
消费金额为 0 的用户不进入排行
防止刷量：仅统计真实扣费成功的记录

11. 开发优先级P0：公开 API + 脱敏逻辑 + 侧边栏入口 + 基础页面
P1：Redis 实时排行缓存
P2：前三名视觉优化 + 移动端适配
P3：管理后台开关与统计

此文档可直接作为二开需求使用。如果需要我继续补充：完整 Go Service 代码示例
Redis 维护逻辑
前端组件完整代码
